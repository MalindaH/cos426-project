// import { Group } from 'three';
// import * as THREE from 'three';

// class OrthoCamera extends Group {
//     constructor(frustumSize, aspect) {
//         // Invoke parent Group() constructor with our args
//         super();

//         this.state = {
//             cam: null,
//         };

//         // console.log("hi");
//         // const frustumSize = 1500;
//         // const aspect = window.innerWidth / window.innerHeight;

//         const camera = new THREE.OrthographicCamera(frustumSize * aspect / - 2, frustumSize * aspect / 2, frustumSize / 2, frustumSize / - 2, -100, 3000);
//         camera.position.set(-3,7,-5);
//         camera.zoom = 70;

//         this.state.cam = camera;
//         this.add(camera);

//     }
// }

// export default OrthoCamera;
